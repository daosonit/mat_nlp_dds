sudo apt-get update
sudo apt-get install -y gfortran libopenblas-dev liblapack-dev
sudo apt update
sudo apt install cmake build-essential

# 2. Cài đặt ngôn ngữ Rust

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
