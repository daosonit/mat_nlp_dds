# ==========================================
# CẤU HÌNH PRODUCTION CHO API BACKEND
# Copy file này thành `.env` bên trong thư mục API
# ==========================================

# Chuỗi kết nối Database (dùng tên container "db" của docker-compose)
DATABASE_URL=postgresql+asyncpg://postgres:mat_super_secret_2026@db:5432/cars

# Chuỗi kết nối RabbitMQ (dùng tên container "rabbitmq")
RABBITMQ_URL=amqp://matgroup_rmq:matgroup_rmq123@rabbitmq:5672/

# Khóa bí mật dùng để mã hóa token đăng nhập (JWT)
JWT_SECRET=HjfjxLDhaj2XzP1QXWG2p4Xjjdn

# N8N & AI Server config
N8N_API_KEY=matgroup_n8n_secret_2026
AI_SERVER_URL=http://ai_worker:8060/predict

# Thư mục lưu trữ model AI (sử dụng chung cho AI Worker và API)
# LƯU Ý: Nếu đổi thư mục này, BẮT BUỘC phải đổi đích mount volume 'ai_models_cache' trong docker-compose.yml
AI_MODELS_DIR=/app/ai_models
